/**
 * effects/scores.js — DEPRECATED (v2.0)
 *
 * 请使用 AnimationController (js/animation/Controller.js) 替代:
 *   - ac.countUp(el, target)  → 数字滚动
 *   - ac.fillBar(el, percent)  → 进度条
 *   - ac.stagger(elements, { preset: 'fadeIn-stagger' }) → 建议列表
 *   - ac.enter(el, { preset: ... }) → 页面入场
 *
 * 保留此文件仅用于向后兼容。
 */
export function animateCounter() { return null; }
export function animateDimensionBars() { return null; }
export function animateAdviceList() { return null; }
export function animateReportEntrance() { return null; }
