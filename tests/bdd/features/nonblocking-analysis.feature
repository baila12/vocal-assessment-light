Feature: 非阻塞分析体验 — 分析中可操作、可播放、可查看实时音准
  As a 声乐学生
  I want to 在音频分析进行中就能播放音频、查看实时音准
  So that 不用干等进度条, 等待时间也能用来听自己的演唱

  Background:
    Given Flask 服务已启动
    And 前端支持 SSE (Server-Sent Events) 接收实时进度

  # ═══════════════════════════════════════════════════════════
  # 一、非阻塞进度 — 核心 UX 变更
  # ═══════════════════════════════════════════════════════════

  Scenario: 分析进度条不遮挡页面
    Given 我已上传一个音频文件
    When 我点击 "开始分析"
    Then 不应弹出全屏遮罩或模态框
    And 进度指示器应显示为:
      | 位置       | 形式                                   |
      | 页面顶部   | 细条进度条 (4px 高, 主色 #3b82f6)      |
      | 评分卡片区 | 各维度图标依次亮起 (音准→节奏→气息→技术→艺术) |
      | 状态文字   | "正在分析音准..." → "正在分析节奏..."     |
    And 页面其余部分应完全可交互 (按钮不置灰, 链接可点击)
    And 用户应可以:
      | 操作             | 是否可用 |
      | 播放已上传的音频 | ✅ 可用  |
      | 查看历史记录     | ✅ 可用  |
      | 切换到其他页面   | ✅ 可用  |
      | 重新上传音频     | ❌ 不可用 (当前分析中) |
      | 重复点击开始分析 | ❌ 不可用 (防重复提交) |

  Scenario: 分析过程中切换页面后回来
    Given 分析正在进行中 (进度 60%)
    When 我点击导航栏 "历史记录"
    Then 应跳转到历史记录页面
    And 顶部应保留一条迷你进度条 (2px, 持续更新)
    And 分析在后台继续执行 (不中断)
    When 我返回首页
    Then 若分析已完成 → 直接展示评分结果
    And 若分析仍在进行 → 恢复完整进度条显示
    And 不应重新开始分析

  Scenario: 分析进度实时推送 (SSE)
    Given 我已点击 "开始分析"
    When 后端开始处理
    Then 应建立 SSE 连接 (/api/analysis/progress?task_id=xxx)
    And 后端应按以下顺序推送事件:
      | 事件              | 进度 | 数据                            |
      | voice_check       | 5%   | is_voice: true/false            |
      | feature_pitch     | 15%  | 基频曲线采样点 (用于实时音准)    |
      | feature_rhythm    | 30%  | onset 序列                      |
      | feature_breath    | 45%  | HNR/RMS 摘要                    |
      | feature_technique | 60%  | 技巧检测结果                     |
      | scoring           | 80%  | 各维度初步评分                   |
      | matching          | 85%  | 标准曲库匹配结果 (如有)          |
      | complete          | 100% | 完整评分结果 JSON                |
      | error             | —    | 错误信息                        |
    And 前端应在收到 feature_pitch 事件后立即渲染音准曲线
    And 在收到 complete 事件后平滑过渡到完整结果视图

  # ═══════════════════════════════════════════════════════════
  # 二、分析中即可播放音频
  # ═══════════════════════════════════════════════════════════

  Scenario: 上传后立即可播放 — 不等分析完成
    Given 我上传了一个音频文件
    And 音频已保存到服务端 (上传完成, 分析未开始或进行中)
    When 我点击 "播放" 按钮
    Then 音频应立即开始播放 (不需等待分析)
    And 播放控件应完全可用: 播放/暂停/进度条/音量
    And 此时分析可能在后台并行运行

  Scenario: 分析中播放 — 无参考时显示用户音高曲线
    Given 分析正在进行中 (已收到 feature_pitch 事件, 基频数据可用)
    And 未匹配到标准歌曲
    When 我点击播放音频
    Then 应切换到 "音准视图"
    And 应显示单条用户音高曲线 (蓝色 #3b82f6)
    And 曲线应随播放进度从右向左滚动
    And 视图标注 "绝对音高 (分析中…)" + 迷你旋转动画
    And 评分维度卡片显示 "音准 ✓ 已提取 | 节奏 ⏳ 分析中… | …"

  Scenario: 分析中播放 — 有参考时显示双曲线实时对比
    Given 分析正在进行中
    And 系统已在 matching 阶段匹配到标准歌曲
    And 标准歌曲的基频数据已从曲库加载
    When 我点击播放音频
    Then 应切换到 "音准对比视图"
    And 应显示双曲线: 标准 (#6366f1 虚线) + 用户 (绿/橙/红着色)
    And 偏差着色应在收到 scoring 事件后更新 (初始可用基频数据粗略着色)
    And 视图标注 "参考: XXX — 分析中…"
    And 评分结果出来后, 统计面板应平滑出现 (精准率/略偏/跑调)

  Scenario: 分析中播放 — 音准数据尚未就绪
    Given 我上传了音频并点击了播放
    And 分析刚开始, feature_pitch 事件尚未到达
    When 音准视图尝试渲染
    Then 应显示 "音准数据加载中…" 占位动画 (脉冲波纹)
    And 播放进度条仍正常工作
    And 收到 feature_pitch 事件后, 占位动画平滑过渡为实际曲线
    And 过渡应在 500ms 内完成

  # ═══════════════════════════════════════════════════════════
  # 三、分析完成后 — 无缝过渡
  # ═══════════════════════════════════════════════════════════

  Scenario: 分析完成时正在播放 — 无打断
    Given 我正在播放音频并查看音准曲线 (分析进行中)
    When 后端推送 complete 事件 (分析 100% 完成)
    Then 音频播放不应中断或暂停
    And 音准曲线不应闪烁或重新渲染
    And 评分卡片应从 "分析中…" 平滑过渡为最终分数 (0.5s 动画)
    And 雷达图应从灰色占位填充为实际数据 (展开动画)
    And 建议列表应从空状态逐条出现 (错开 100ms)

  Scenario: 分析完成时在历史页面 — 通知
    Given 分析在后台完成 (用户当前在历史记录页面)
    When 收到 complete 事件
    Then 导航栏 "首页" 应有红点徽标
    And 页面标题应闪烁一次 "分析完成 — 查看结果"
    And 用户切回首页时直接看到完整结果 (无需等待)

  Scenario: 分析失败时 — 非阻塞错误提示
    Given 分析过程中发生错误 (如 Demucs 分离失败)
    When 后端推送 error 事件
    Then 不应弹出模态框或 alert
    And 顶部进度条应变为红色
    And 应显示 Toast 通知: "分析失败: xxx, 点击重试"
    And Toast 应在 5 秒后自动消失
    And 已提取的音准数据仍可用于音准视图 (如果 feature_pitch 已推送)
    And "重新分析" 按钮应重新启用

  # ═══════════════════════════════════════════════════════════
  # 四、分析中重新选择模式
  # ═══════════════════════════════════════════════════════════

  Scenario: Quick 分析进行中切换为 Pro 模式
    Given 我以 Quick 模式开始了分析 (进度 50%)
    When 我意识到需要 Pro 模式, 点击 "切换到专业模式"
    Then 当前分析应被取消 (发送 cancel 事件)
    And 应弹出确认: "当前分析将被放弃, 是否切换?"
    And 确认后重新以 Professional 模式开始分析
    And 进度条重置为 0%

  Scenario: 分析中取消
    Given 分析正在进行中 (进度 40%)
    When 我点击进度条旁边的 ✕ 取消按钮
    Then 应发送 cancel 请求到后端
    And 后端应中断当前分析任务
    And 进度条应消失
    And 已提取的音准数据应保留 (可用于回放)
    And "开始分析" 按钮应重新启用
