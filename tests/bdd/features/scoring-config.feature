Feature: 评分权重可配置 — 风格预设 + 用户自定义 + 系统推荐
  As a 声乐学生或教师
  I want to 根据不同风格和需求调整评分维度的权重
  So that 评分更符合特定歌曲或演唱场景的侧重点

  Background:
    Given FastAPI 服务已启动
    And 评分配置文件 scoring_config.py 中定义了风格预设

  # ═══════════════════════════════════════════════════════════
  # 一、风格预设 — 开箱即用的默认值
  # ═══════════════════════════════════════════════════════════
  # v7.11 适配: 原始契约为 5 维 (muscle 维度前编写)。
  # 现为 6 维 — 原 5 维按比例 ×0.85, muscle 保持默认 15%。
  # 权威值见 ScoringWeights.presets()。

  Scenario: 系统提供四种风格预设权重
    Given 评分系统已加载默认配置
    When 我查询可用的风格预设
    Then 应返回以下预设:
      | 风格   | Pitch | Rhythm | Breath | Technique | Muscle | Artistry | 说明                       |
      | 流行   | 21%   | 17%    | 13%    | 17%       | 15%    | 17%      | 均衡, 艺术表现权重较高       |
      | 美声   | 25%   | 13%    | 21%    | 17%       | 15%    | 9%       | 偏重音准和气息, 节奏次之     |
      | 民族   | 24%   | 15%    | 15%    | 15%       | 15%    | 16%      | 五维均衡, 各有侧重           |
      | 说唱   | 8%    | 30%    | 9%     | 13%       | 15%    | 25%      | 节奏和艺术表现是核心, 音准次要 |
    And 每种预设应有对应的阈值微调参数 (PitchThresholds MAE断点等)
    And 默认使用 "流行" 预设 (如用户未指定)

  Scenario: 选择风格预设后各维度阈值联动调整
    Given 我选择了 "美声" 风格预设
    When 系统应用该预设
    Then 不仅权重变化, 各维度的阈值也应联动:
      | 维度     | 流行默认         | 美声默认           | 变化原因             |
      | Pitch    | MAE≤12→专业级   | MAE≤10→专业级     | 美声对音准要求更严    |
      | Breath   | 长音≥3s→优秀    | 长音≥5s→优秀      | 美声需要更长气息支撑  |
      | Rhythm   | 偏差≤12%→专业级 | 偏差≤15%→专业级   | 美声Rubato弹性更大    |
    And 阈值变化应记录在评分结果中 (applied_preset + threshold_overrides)

  Scenario: 未指定风格时自动检测
    Given 我上传了一首没有指定风格的歌曲
    When 系统分析音频特征
    Then 应根据以下特征自动推荐风格:
      | 特征                     | 倾向风格 |
      | BPM 稳定 + 强节拍 + 语速快 | 说唱     |
      | 长音 + 颤音 + 高HNR       | 美声     |
      | 中等BPM + 常规音域         | 流行     |
      | 滑音多 + 特殊音阶          | 民族     |
    And 自动检测结果标注 confidence (如 "流行, 置信度 0.85")
    And 用户可在分析前修改自动检测的结果

  # ═══════════════════════════════════════════════════════════
  # 二、用户自定义权重 — 录入歌曲 / 对比分析时
  # ═══════════════════════════════════════════════════════════

  Scenario: 录入标准歌曲时自定义评分权重
    Given 我正在向标准曲库添加一首歌曲 "青藏高原"
    And 这首歌是一首高难度美声作品
    When 我填写歌曲元数据时展开 "评分参数设置"
    Then 应显示:
      | 风格选择器       | 下拉: 流行/美声/民族/说唱/自定义 |
      | 五维权重滑块     | 每个维度 0-50%, 总和自动归一化    |
      | 系统推荐按钮     | "根据音频特征推荐最优权重"        |
      | 预设加载按钮     | "加载美声默认值"                 |
    And 选择风格后, 滑块自动填充该风格的默认值
    And 切换到 "自定义" 后可自由拖动滑块
    And 五维权重总和必须为 100% (前端实时校验 + 后端验证)
    And 保存后, 该歌曲的评分参数存在数据库的 scoring_config 字段

  Scenario: 对比分析时自定义权重
    Given 我已上传标准音频和用户音频
    When 我点击 "开始对比分析" 前展开 "评分参数设置"
    Then 应显示与录入歌曲时相同的参数面板
    And 默认加载标准音频关联的风格预设 (如果该歌曲在曲库中)
    And 如果标准音频不在曲库中 → 默认使用自动检测的风格预设
    And 修改参数后点击 "应用并分析" → 以自定义参数执行评分

  Scenario: 权重总和校验
    Given 我在自定义权重面板中
    When 我设置: Pitch=30%, Rhythm=30%, Breath=25%, Technique=20%, Artistry=10%
    Then 总和为 115%, 超过 100%
    And "应用" 按钮应置灰
    And 应显示红色提示 "权重总和 115%, 需恰好为 100%"
    And 我调整某个滑块后总和回到 100% → 按钮自动启用

  Scenario: 权重滑块联动 — 归一化模式
    Given 我在自定义权重面板中启用了 "自动归一化" 开关
    When 我将 Pitch 从 28% 调到 35%
    Then 其他四个维度应按比例自动缩减, 保持总和 100%
    And 缩减比例应保持剩余维度的相对权重不变
    And 关闭 "自动归一化" 后 → 手动模式 (总和超限则报错)

  # ═══════════════════════════════════════════════════════════
  # 三、系统推荐权重 — 基于音频特征分析
  # ═══════════════════════════════════════════════════════════

  Scenario: 系统分析标准音频后推荐权重
    Given 我上传了一首标准歌曲 "月亮代表我的心"
    When 我点击 "系统推荐权重"
    Then 系统应分析音频特征并返回推荐值:
      | 分析维度       | 发现                         | 推荐影响                     |
      | 音域范围       | C3-E5 (2个八度+2度)          | 中等 → Pitch 权重不变        |
      | 节奏复杂度     | BPM=78, 4/4拍, 节奏规整      | 简单 → Rhythm 权重 -5%       |
      | 长音占比       | 35% 乐句含≥2s长音            | 较多 → Breath 权重 +5%       |
      | 技巧特征       | 颤音3处, 滑音8处, 无假声     | 中等 → Technique 权重不变    |
      | 情感表达维度   | 动态范围 25dB, 气声使用适度   | 丰富 → Artistry 权重 +5%     |
    And 推荐结果应显示 "调整理由" (每项权重变化的文字解释)
    And 推荐值应作为初始值填入滑块 (用户可进一步修改)
    And 推荐耗时应在 5 秒内完成

  Scenario: 系统推荐 + 用户微调 → 保存为自定义预设
    Given 系统给出了推荐权重: Pitch=24%, Rhythm=18%, Breath=24%, Tech=18%, Art=16%
    When 我手动将 Artistry 从 16% 调到 20%, 其他微调
    And 点击 "保存为自定义预设"
    Then 应弹出命名对话框
    And 我输入 "月亮代表我的心 - 情感版"
    And 该预设应保存到本地 (JSON)
    And 下次分析时可从 "我的预设" 下拉中加载

  Scenario: 推荐权重 vs 实际评分结果的一致性反馈
    Given 我使用系统推荐的权重完成了一次评分
    When 我查看评分结果
    Then 结果页应显示:
      | 当前使用的权重配置 (风格 + 各维度%) |
      | "使用其他预设对比" 按钮 → 下拉: 流行/美声/民族/说唱 |
    And 点击其他预设后 → 用新权重重新计算总分 (不上传, 纯前端)
    And 显示对比: "流行预设: 78 分 | 美声预设: 72 分 | 自定义: 80 分"
    And 用户可直观感受不同权重对总分的影响

  # ═══════════════════════════════════════════════════════════
  # 四、边界条件
  # ═══════════════════════════════════════════════════════════

  Scenario: 极端权重 — 某维度设为 0%
    Given 我在自定义权重中将 Breath 设为 0%
    When 我点击 "应用并分析"
    Then 系统应弹出确认: "气息维度权重为 0%, 该维度将不参与总分计算。确认?"
    And 确认后, Breath 评分仍会计算 (用于诊断) 但不计入 total_score
    And 结果页 Breath 卡片标注 "不计入总分"

  Scenario: 某维度权重超过 50%
    Given 我在自定义权重中将 Pitch 设为 55%
    When 我尝试应用
    Then 应拒绝并提示 "单个维度权重不能超过 50%"
    And 避免单维度主导评分 (防止用户误操作)

  Scenario: 配置文件损坏或缺失 — 启动即报错
    Given config/styles.yaml 被意外删除或内容损坏 (YAML 解析失败)
    When 系统启动时加载配置
    Then 应抛出 ConfigError 并终止启动
    And 错误信息应明确指明: "config/styles.yaml 缺失或格式错误: <具体原因>"
    And /health 端点不可用 (服务未启动)
    And 不应静默回退到任何硬编码值
    And 修复配置文件后重新启动即可恢复

  Scenario: 多人协作 — 预设导入导出
    Given 我创建了几个自定义权重预设
    When 我点击 "导出预设"
    Then 应下载一个 JSON 文件 (含预设名称 + 权重 + 阈值覆盖)
    And 其他用户可在设置页 "导入预设" 上传该 JSON
    And 导入后预设出现在 "我的预设" 列表中
    And 导入时检测重名 → 提示 "预设 'xxx' 已存在, 是否覆盖?"
