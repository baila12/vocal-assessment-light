Feature: 实时音准对比显示 — 全模式通用
  As a 声乐学生
  I want to 在音频播放时看到我的音高与标准音高的实时对比
  So that 像全民K歌一样直观地看到哪里唱准了、哪里跑调了

  Background:
    Given 服务已启动
    And 浏览器支持 Web Audio API 和 Canvas
    And 前端已加载 YIN 音高检测模块 (pitch-detector.js)

  # ═══════════════════════════════════════════════════════════
  # 一、通用视觉规范 (所有模式共用)
  # ═══════════════════════════════════════════════════════════

  Scenario: 音准曲线基本渲染
    Given 一个已完成评估的音频文件 (含预提取的基频曲线)
    When 我点击 "播放" 并切换到 "音准对比" 视图
    Then 应在 Canvas 上渲染两条曲线:
      | 曲线          | 颜色     | 线宽 | 说明                 |
      | 标准音高曲线  | #6366f1  | 2px  | 参考音高 (虚线)       |
      | 用户音高曲线  | 动态着色 | 3px  | 实时渲染 (实线)       |
    And 曲线应从右向左滚动 (类似全民K歌)
    And 当前播放位置应在画面中央 (竖线标记)
    And Y 轴标注音高 (C3-B5, 钢琴键白键高亮)
    And X 轴显示时间刻度 (秒)

  Scenario: 音准偏差颜色映射
    Given 用户音高与标准音高存在不同程度的偏差
    When 实时曲线渲染到不同段落
    Then 用户曲线应按以下规则着色:
      | 偏差范围       | 颜色     | 含义         |
      | ≤ 25 音分      | #22c55e  | 精准 (绿色)  |
      | 25-50 音分     | #f59e0b  | 略偏 (橙色)  |
      | > 50 音分      | #ef4444  | 跑调 (红色)  |
    And 颜色应在音符边界处平滑过渡 (非逐帧跳变)
    And 人声未发声段落应显示为灰色虚线 (#94a3b8)

  Scenario: 无标准音频时仅显示用户曲线
    Given 系统未匹配到标准歌曲, 也没有手动指定参考音频
    When 我播放已评估的音频并切换到音准视图
    Then 应只渲染一条用户音高曲线 (蓝色 #3b82f6)
    And 不显示颜色偏差映射 (因为没有参考)
    And 视图标题应标注 "绝对音高 (无参考)"
    And Y 轴仍显示音高刻度

  # ═══════════════════════════════════════════════════════════
  # 二、边界条件 — 数据质量
  # ═══════════════════════════════════════════════════════════

  Scenario: 清辅音/气声段落 — 基频检测失败
    Given 音频中存在气声段落 (如 "h", "s", "f" 辅音)
    When 播放到该段落
    Then 基频检测应返回置信度 < 0.5
    And 该段落应显示为虚线空白 (颜色 #94a3b8, 透明度 40%)
    And 不应标记为跑调 (红色) — 因为不是音高错误, 是检测不到
    And 空白段落在 Y 轴上的位置应延续上一个有效音高 (不跳变)

  Scenario: 八度跳变误检测
    Given PYIN 算法在某些段落产生八度误差 (检测到基频的 2 倍频)
    When 显示该段落的音高曲线
    Then 系统应检测到八度跳变 (相邻帧音高跳变 ≥ 12 半音)
    And 应在曲线上方显示 ⚠️ 标记 "八度跳变 (可能误检)"
    And 该段落不应标记为红色跑调
    And 该段落的颜色应使用灰色 (表示不确定)

  Scenario: 音频首尾静音
    Given 音频开头有 0.5 秒静音, 结尾有 1 秒静音
    When 渲染完整的音高曲线
    Then 首尾静音段应自动裁剪 (不显示或显示为空白)
    And 第一条有效音高的位置应对齐到时间轴 0:00
    And 不应将静音段算作 "跑调"

  Scenario: 短音频 (不足 10 秒)
    Given 一个仅 5 秒长的短音频
    When 播放并显示音准对比
    Then Canvas 应自动缩放到填满视口宽度
    And 时间轴刻度应调整为秒级精度 (每格 1 秒)
    And 曲线不应被裁剪

  Scenario: 长音频 (超过 3 分钟)
    Given 一个 4 分钟的长音频
    When 播放并显示音准对比
    Then 应使用视口窗口 (可见范围约 15 秒)
    And 曲线应随播放位置平滑滚动 (不是跳帧)
    And 底部应有缩略导航条 (全长波形预览 + 当前视口高亮)
    And 拖动缩略条可跳转到任意位置

  # ═══════════════════════════════════════════════════════════
  # 三、边界条件 — 播放控制
  # ═══════════════════════════════════════════════════════════

  Scenario: 暂停时冻结曲线
    Given 我正在播放音频并查看实时音准对比
    When 我点击 "暂停"
    Then 曲线滚动应立即停止
    And 当前帧的偏差着色应保持显示
    And 播放位置竖线应保持不动
    And 恢复播放后应从暂停位置继续滚动 (无跳跃)

  Scenario: 拖拽进度条跳转
    Given 我正在查看音准对比视图
    When 我拖拽播放进度条到 1:30 位置
    Then 音准曲线应立即跳转到对应时间点
    And 不应出现曲线从 0:00 重新滚动的动画
    And 偏差着色应瞬间更新为新位置的数据
    And 跳转响应应在 200ms 内完成

  Scenario: 倍速播放 (0.5x / 1.5x)
    Given 音频以 0.5 倍速播放
    When 我查看音准对比视图
    Then 曲线滚动速度应同步减半
    And 偏差着色不应受影响 (仍然逐帧计算)
    And 时间轴刻度应保持不变 (显示真实时间, 非播放时间)
    And 1.5 倍速时滚动加速, 但渲染帧率保持 ≥ 30fps

  Scenario: 循环播放某一段落
    Given 我设置了 A-B 循环 (1:00-1:30)
    When 播放循环回到 A 点
    Then 曲线应从 A 点重新开始渲染
    And 不应出现残影或旧曲线残留
    And 循环过渡应无缝 (无闪烁)

  # ═══════════════════════════════════════════════════════════
  # 四、边界条件 — 性能与兼容性
  # ═══════════════════════════════════════════════════════════

  Scenario: 渲染帧率保障
    Given 任何播放速度下的音准对比视图
    Then Canvas 渲染帧率应 ≥ 30fps
    And 不应造成音频播放卡顿或音画不同步
    And 音画同步误差应 < 50ms
    And 若浏览器帧率低于 30fps, 应自动降至 15fps (降级渲染)

  Scenario: 低性能设备降级
    Given 设备 CPU 性能不足 (用户可感知的卡顿)
    When 系统检测到连续 3 秒帧率 < 20fps
    Then 应自动切换为 "性能模式":
      | 优化项           | 效果                           |
      | 曲线抗锯齿       | 关闭 (锯齿边缘)                |
      | 偏差着色颗粒度   | 从逐帧改为每 3 帧一次          |
      | 背景网格线       | 关闭                           |
      | 缩略导航条       | 关闭                           |
    And 应在界面显示 "性能模式 (可手动关闭)"
    And 用户可手动切换回 "画质模式"

  Scenario: 浏览器标签页不可见时暂停渲染
    Given 我正在查看音准对比视图
    When 我切换到另一个浏览器标签页
    Then Canvas 渲染应暂停 (requestAnimationFrame 自动节流)
    And 音频播放应继续 (不中断)
    And 切换回标签页时, 曲线应瞬间更新到当前播放位置
    And 不应出现 "追赶式" 高速滚动

  Scenario: 窗口缩放时自适应
    Given Canvas 宽度为 800px
    When 我调整浏览器窗口大小到 1200px
    Then Canvas 应重新计算尺寸并重绘
    And 不应出现拉伸变形
    And 曲线细节应保持清晰 (重新按新宽度采样)
    And 响应时间 < 200ms

  # ═══════════════════════════════════════════════════════════
  # 五、模式特定交互
  # ═══════════════════════════════════════════════════════════

  Scenario: Quick/Pro 模式 — 回放时查看音准 (无参考)
    Given 我用 Quick 模式上传了一首曲库中不存在的歌曲
    And 评估已完成 (无 matched_song)
    When 我切换到 "音准视图" 并播放
    Then 应显示单条用户音高曲线 (蓝色, 无对比)
    And 视图顶部标注 "无参考音频 — 显示绝对音高"
    And 应在曲线上标注 "最高音: G5" "最低音: C3"

  Scenario: Quick/Pro 模式 — 回放时查看音准 (有自动匹配)
    Given 我用 Quick 模式上传了 "月亮代表我的心" 翻唱
    And 系统自动匹配到标准歌曲 (置信度 0.85)
    When 我切换到 "音准视图" 并播放
    Then 应显示双曲线对比 (标准 vs 用户)
    And 视图顶部标注 "参考: 月亮代表我的心 - 邓丽君"
    And 偏差着色 (绿/橙/红) 应基于与标准音高的差距
    And 播放结束后应显示统计: "精准率 78% | 略偏 15% | 跑调 7%"

  Scenario: 选歌录音 — 录音中实时对比
    Given 我已选择 "月亮代表我的心" 作为标准歌曲
    And 已进入录音模式
    When 我开始录音并演唱
    Then 应在 Canvas 上实时显示:
      | 元素             | 说明                                 |
      | 标准音高虚线     | 随时间从右向左滚动 (与伴奏同步)       |
      | 用户实时音高点   | 每个音符渲染为 3px 圆点               |
      | 偏差背景色带     | 标准线上下 25/50 音分标注绿色/橙色区域 |
      | 当前音分偏差数值 | 右上角大号数字 "+15 音分"             |
      | 音高趋势箭头     | 偏高 ↑ (红) / 偏低 ↓ (蓝) / 精准 ✓ (绿) |
    And 圆点在 2 秒后淡出 (保留最近的音高轨迹)
    And 不应显示完整的用户曲线 (因为还没唱完)

  Scenario: 选歌录音 — 录音后回放对比
    Given 我刚录完一段 "月亮代表我的心" 的演唱
    When 我点击 "查看回放" 并播放
    Then 应显示完整的双曲线对比 (与 Scenario "回放时查看音准 (有自动匹配)" 一致)
    And 问题段落应用红色半透明背景高亮 (偏差 > 50 音分持续 > 0.5s)
    And 点击问题段落可跳到该位置重新播放
    And 应显示逐句音准评分 (每句一个分数标签浮在曲线上方)

  Scenario: 对比分析 — 双轨叠加对比
    Given 我手动上传了标准音频和用户音频
    And DTW 对齐已完成
    When 我播放对比结果
    Then 应显示双曲线叠加 (经过 DTW 时间对齐)
    And 两条曲线使用不同颜色 (标准: #6366f1 虚线, 用户: 动态着色实线)
    And 偏差区域应在标准曲线上下标注填色:
      | ≤ 25 音分 | 浅绿半透明填充  |
      | 25-50     | 浅橙半透明填充  |
      | > 50      | 浅红半透明填充  |
    And 底部应显示偏差热力图条 (一整行, 颜色密度表示跑调程度)
    And 点击偏差热力图任意位置可跳转播放

  Scenario: 对比分析 — DTW 未对齐段落标记
    Given DTW 对齐在某些段落置信度低 (< 0.5)
    When 渲染音准对比视图
    Then 低置信度段落应在曲线上方标记 "⚠️ 对齐不确定"
    And 该段落的偏差着色应使用灰色虚线 (不误导用户)
    And 统计面板应排除低置信度段落的 "跑调率" 计算

  # ═══════════════════════════════════════════════════════════
  # 六、辅助功能
  # ═══════════════════════════════════════════════════════════

  Scenario: 切换显示模式
    Given 我正在查看音准对比视图 (双曲线模式)
    When 我点击 "仅显示用户曲线"
    Then 标准曲线应隐藏
    And 用户曲线切换为蓝色 (无偏差着色)
    And 再次点击 "显示对比" → 恢复标准曲线 + 偏差着色
    And 这个切换不应中断播放

  Scenario: 导出音准对比截图
    Given 音准对比视图显示了一段典型的跑调段落
    When 我点击 "截图" 按钮
    Then 应导出当前 Canvas 内容为 PNG 图片
    And 图片应包含当前时间戳水印 "01:23 / 03:45"
    And 图片分辨率应为 Canvas 实际分辨率 (不失真)
    And 可附加到评估报告中

  Scenario: 键盘快捷键
    Given 音准对比视图处于激活状态
    When 我按下以下按键:
      | 按键 | 行为                 |
      | Space| 播放/暂停            |
      | ←    | 后退 5 秒            |
      | →    | 前进 5 秒            |
      | R    | 切换参考曲线显示/隐藏 |
      | S    | 截图                 |
      | 1    | 仅显示用户曲线       |
      | 2    | 显示双曲线对比       |
    Then 对应行为应立即生效
    And 不应与浏览器默认快捷键冲突
