# ============================================================
# 声乐评估系统 - Web版依赖 (web_app.py)
# ============================================================
# 安装: pip install -r requirements.txt
# 注意: torch 和 torchaudio 建议先用 conda 安装，见下文说明

# ---- Web 框架 ----
Flask>=3.0
flask-cors>=4.0

# ---- 数据验证 ----
pydantic>=2.0

# ---- 科学计算 ----
numpy>=1.21.0
scipy>=1.7.0

# ---- 音频处理 ----
librosa>=0.10.0
soundfile>=0.12.0
pyyaml>=6.0

# ---- 可视化 ----
matplotlib>=3.5.0

# ---- 深度学习 (懒加载，按需导入) ----
# torch / torchaudio 建议优先用 conda 安装:
#   conda install pytorch torchaudio cpuonly -c pytorch
# 或者 CPU 版 pip:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchaudio>=2.0.0
torchcrepe>=0.0.19
transformers>=4.30.0
speechbrain>=0.5.14
onnxruntime>=1.15.0

# ---- 人声分离 ----
demucs>=4.0

# ---- 桌面GUI (main.py) 额外依赖 (可选) ----
# PySide6>=6.4.0
# pyqtgraph>=0.13.0
# sounddevice>=0.4.0
